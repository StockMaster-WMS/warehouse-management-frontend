import { LoginForm } from "./login-form";

export const metadata = {
  title: "Đăng nhập | StockMaster",
  description: "Đăng nhập vào hệ thống quản lý kho StockMaster.",
};

export default function LoginPage() {
  return <LoginForm />;
}
